import { IconName, LinePattern } from '../../../../../interfaces';

const options: [LinePattern, IconName][] = [
    [0, 'pattern1'],
    [1, 'pattern2'],
    [2, 'pattern3'],
    [3, 'pattern4'],
];

export default options;
